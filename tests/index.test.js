const { expect } = require('chai');
const sinon = require('sinon');
const index = require('../index.js');

describe('Index', () => {
  it('Should start a Websocket server');
  it('Should setup a broadcast function in the WS instance which sends a message to all open connections');
});
